import React from 'react';
import TextField from '@material-ui/core/TextField';

const Form = () => {
    return (
        <div>
            <TextField />
        </div>
    );
}

export default Form;